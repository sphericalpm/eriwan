version: '3'
services:
  flask:
    image: webapp-flask
    build:
      context: .
      dockerfile: Dockerfile-flask
    volumes:
      - /www/Eriwan/app/static/jingles:/app/app/static/jingles
      - /www/Eriwan/media:/app/media
      - /www/Eriwan/app.db:/app/app.db
  nginx:
    image: webapp-nginx
    build:
      context: .
      dockerfile: Dockerfile-nginx
    ports:
      - 5000:80
    depends_on:
      - flask